# Story 2.2 — Domain Enforcement

**Status:** Review (2026-08-11)
**PRD:** [Epic 2](../requirements/prd.md#epic-2--authentication--user-provisioning) · FR2, SEC-2/SEC-3

Three layers live: (1) DB trigger gate from Story 1.4 (authoritative — rejected identities never get an auth user; CI-verified negative test); (2) callback backstop `src/app/auth/callback/route.ts` — verifies exact-domain email via `isAllowedEmail` (EC-A3-proof, unit-tested incl. `sub.folio3.com`, `folio3.com.evil.com`, quoted-local-part tricks) and a provisioned profile row, else signOut + `/access-denied`; (3) RLS `is_provisioned()` in every policy. Access-denied page with per-reason copy (EC-A1).
