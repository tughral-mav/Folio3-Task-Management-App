# Story 2.1 — Google Sign-in

**Status:** Review (2026-08-11) — code complete; live OAuth pending Google client config
**PRD:** [Epic 2](../requirements/prd.md#epic-2--authentication--user-provisioning) · FR1, FR6

`/login` is the only auth surface (`GoogleSignInButton` → `signInWithOAuth`, `hd` hint UX-only per SEC-2). Sessions via `@supabase/ssr`: browser + server clients (`src/lib/supabase/`), refresh in `src/proxy.ts` (Next 16 proxy convention — Node runtime). Sign-out server action everywhere in the authenticated shell. No signup/password surface exists.

Blocked on human step: Google OAuth client creation + Supabase Auth provider config (instructions provided at Epic 2 report; verified live in Test 1's manual portion).
