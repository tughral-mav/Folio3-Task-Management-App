# Story 5.2 — Unread Badge

**Status:** Review (2026-08-11)
**PRD:** [Epic 5](../requirements/prd.md#epic-5--notifications--realtime) · FR29, FR32

`NotificationBell` in the global header: server-rendered unread count (99+ cap), hidden at zero, screen-reader label announces the count (NFR5). Kept live by `RealtimeRefresher`: RLS-filtered `postgres_changes` subscriptions on the caller's notifications + tasks → debounced `router.refresh()`; window-focus refetch covers websocket drops (EC-N5). Works on mobile (touch-sized target in the sticky header).
