# Story 2.4 — Role-Based Routing & Guards

**Status:** Review (2026-08-11)
**PRD:** [Epic 2](../requirements/prd.md#epic-2--authentication--user-provisioning) · FR5, FR9

`getSessionProfile` (React-cached, per-request DB read — SEC-5, EC-R2), `requireUser`, `requireAdmin` in `src/lib/auth/session.ts`. `/` routes by DB role → `/admin` or `/my` (FR5); `admin/layout.tsx` guards the whole ADMIN subtree server-side; proxy handles only coarse authenticated/unauthenticated redirects (architecture §5). Placeholder dashboards render role-appropriate shells so Tests 1/4/5 routing is real; RLS denies admin data to members regardless of any guard.
