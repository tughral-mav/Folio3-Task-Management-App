# Story 1.1 — Project Scaffold

**Status:** Done (2026-08-11)
**PRD:** [Epic 1](../requirements/prd.md#epic-1--foundation--infrastructure)

Next.js 16 (App Router) + TypeScript strict + Tailwind 4, ESLint 9, Vitest 4, Playwright (desktop + 2 mobile device profiles), `output: 'standalone'` + Dockerfile/.dockerignore (NFR9/D5), `.gitignore` keeps `.env.example` committable while blocking real env files. First domain code: `src/lib/utils/dates.ts` (FR36 overdue / due-soon logic) + unit tests; `src/lib/types/domain.ts` enum vocabulary incl. `MEMBER_STATUS_TARGETS` (D3).

**AC check:** `lint`, `typecheck`, `test`, `build` — verified in CI (and locally where the sandbox allows). Repo layout matches governing prompt §43.
