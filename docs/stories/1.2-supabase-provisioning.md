# Story 1.2 — Supabase Provisioning & Local Dev

**Status:** Done (2026-08-11)
**PRD:** [Epic 1](../requirements/prd.md#epic-1--foundation--infrastructure)

Cloud project provisioned via CLI (D6): ref **`acioywfrjbalyqbavmoy`**, region **ap-south-1** (Mumbai — closest to the team), org `tughral-mav's Org`. All seven migrations applied with `supabase db push` (seed is local/CI-only by design). Typed client generated: `src/lib/types/database.ts` (`supabase gen types typescript --linked`). `.env.local` created (gitignored, verified) with URL/anon/service-role keys; DB password stored outside the repo at `%USERPROFILE%\.folio3-taskapp-dbpw.txt`.

Notes:
- First creation attempt had a password-generation failure (PS 5.1); that project was deleted and recreated cleanly — ignore the dead ref `jbtcrzxlsdjvekdtaekg`.
- CLI runs as `npx supabase` (no global install). Local Docker stack unavailable on this machine — CI runners provide the local stack for tests.
- Remaining (Epic 2/Deployment): Google OAuth provider config in Supabase Auth (needs user-created OAuth client), Site URL/redirect allow-list for production.

**AC check:** fresh-clone setup documented (README + .env.example); schema reproducibility proven by the CI schema job and the clean `db push`.
