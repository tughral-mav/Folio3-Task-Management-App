# Story 1.2 — Supabase Provisioning & Local Dev

**Status:** In Progress — cloud provisioning blocked on user's Supabase access token (D6)
**PRD:** [Epic 1](../requirements/prd.md#epic-1--foundation--infrastructure)

Done: `supabase/config.toml`, migration workflow (`supabase db start` / `db reset` applies migrations + seed), deterministic seed exercising the domain gate. CLI runs via `npx supabase` (no global install). Local Docker is unavailable on the dev machine — CI runners provide the local stack; the cloud project is the shared database.

Remaining: create cloud project via CLI (user token), `supabase link`, `db push`, generate typed client (`lib/types/database.ts`), record project ref + region here.
