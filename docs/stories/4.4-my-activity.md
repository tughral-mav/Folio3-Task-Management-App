# Story 4.4 — My Activity / History

**Status:** Review (2026-08-11)
**PRD:** [Epic 4](../requirements/prd.md#epic-4--team-member-experience--progress) · FR25

`/my/activity`: recent events across the member's own tasks (`listRecentActivity`, RLS-scoped — same query serves admins globally later). Member dashboard upgraded to a first FR35 pass: needs-attention section (overdue / blocked / due within 48h via `isOverdue`/`isDueSoon`) + recent tasks. Full stats land in Epic 6.
