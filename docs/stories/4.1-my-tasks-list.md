# Story 4.1 — My Tasks List & Filters

**Status:** Review (2026-08-11)
**PRD:** [Epic 4](../requirements/prd.md#epic-4--team-member-experience--progress) · FR14, FR18

`/my/tasks`: member task list with FR18 filters (status, priority, due-date range — no search/assignee per spec). Visibility is pure RLS — the page adds no "mine" filter, so tampering with query params cannot widen scope. Reuses the responsive `TaskTable` + parameterized `TaskFilters`.
