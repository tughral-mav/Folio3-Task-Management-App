# Story 3.1 — Create & Assign Task

**Status:** Review (2026-08-11)
**PRD:** [Epic 3](../requirements/prd.md#epic-3--task-management-admin) · FR10–FR12

`/admin/tasks/new`: validated form (Zod `taskCreateSchema` — title 1–200, description ≤10k, uuid assignee, priority enum, required due date normalized to end-of-day; SEC-14 server-side). `createTaskAction`: `requireAdmin` at execution + RLS re-check (`created_by` pinned by policy WITH CHECK). Activity + assignee notification fan out in the same DB transaction (ADR-4, Test 6 write path). Assignee picker lists provisioned users only (EC-T1) with an explanatory empty state.
